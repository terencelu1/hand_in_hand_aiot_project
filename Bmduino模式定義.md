1.待機模式:
不斷感測數值(gy906、指紋辨識器、心律血氧模組) 並利用native數值回報給樹梅派(電腦測試) 要跟樹梅派講目前在待機模式
2.工作模式:
當感測到有人按下指紋辨識器 馬上進入這個模式 心律血氧模組會感測數值 gy906繼續感測 並把數據穩定之後將量測到的數值利用native數值回報給樹梅派(電腦測試) 要跟樹梅派講目前在工作模式 如果在15秒感測不到數值 要回報給樹梅派
完成後回到待機模式
3.接收模式
當接收到樹梅派(電腦測試)需要打開甚麼繼電器(1-4)執行動作然後回到待機模式 